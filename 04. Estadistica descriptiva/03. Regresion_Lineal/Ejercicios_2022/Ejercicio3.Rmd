---
title: "Ejercicio3"
author: "Regresión Lineal MDS & MDSF - Afi Escuela"
date: "19/12/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Grupo     | 1 - Betis
--------- | -------------
Miembro 1 | Juan Miguel Jiménez
Miembro 2 | Nabil Fekir
Miembro 3 | Sergio Canales

### Utiliza el dataset Auto, que tienes disponible en el fichero auto.csv o utilizando la librería ISLR. Resuelve las siguientes cuestiones:

**<span style="color:#d84519">Load Dataset</span>**

```{r data}
library(ISLR)
data("Auto")
summary(Auto)
```

**<span style="color:#d84519">1. Revisa, en la ayuda, que es la variable origin. ¿Es una variable numérica? ¿Necesitamos transformarla? Si es así hazlo (utiliza la función ifelse).</span>**

```{r ans1}
# Answer 1
```

**<span style="color:#d84519">2. Utilizando un método de selección de características ajusta un modelo de regresión lineal de mpg, ¿cuál crees que es el modelo óptimo?. Argumenta utilizando R2 ajustado, BIC y Cp. (Utiliza un único método: best, bacward o forward.)</span>**

```{r ans2}
# Answer 2
```

**<span style="color:#d84519">3. Ajusta el modelo que hayas elegido en el apartado anterior y comprueba si verifica las hipótesis de un modelo de regresión lineal. (Diagnóstico del modelo) </span>**

```{r ans3}
# Answer 3
```
