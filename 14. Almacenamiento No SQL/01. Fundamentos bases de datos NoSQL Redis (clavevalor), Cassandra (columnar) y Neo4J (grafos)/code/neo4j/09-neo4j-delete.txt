	// ELIMINACION
//----------------------

// Creamos algunos nodos y relaciones sobre los que trabajar
MATCH
	(promo:Promocion { year: '2018-2019'})
CREATE 
	(:Profesor { name: 'José Manuel Rodríguez'})
	-[:IMPARTE]->
	(:Materia { name: 'Introducción a R'})
	<-[:ASISTE]-
	(promo);
	
// Recuperamos el path completo sobre el que queremos trabajar
MATCH p=(:Profesor)--(:Materia)--(:Promocion) RETURN p;

// Eliminamos una relación
MATCH (:Profesor { name: 'José Manuel Rodríguez'})-[rel:IMPARTE]-()
DELETE rel;

// Eliminamos un nodo
MATCH (jose:Profesor { name: 'José Manuel Rodríguez'}) DELETE jose;

// Eliminamos un path completo
MATCH p=(:Materia)<--(:Promocion) DELETE p; 

// Eliminamos un path completo
MATCH p=(:Materia)<--(:Promocion) DETACH DELETE p; 
